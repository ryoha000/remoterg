# RemoteRG パフォーマンス検証記事 構成案

## タイトル案
- GPUメモリ経路まで追う RemoteRG の低遅延最適化: エンコーダ・キュー・readback の比較検証

## 想定読者
- WebRTC を使ったリアルタイム配信/双方向アプリを実装しているエンジニア
- Rust + Android で低遅延系の性能改善を進めたい人
- 「なんとなく速い」ではなく、計測に基づいて設計判断したい人

## Abstract（案4ベース）
本稿では、遠隔ゲームプレイ基盤 `RemoteRG` の性能最適化を、映像パイプラインと入力パイプラインを分離して検証する。  
映像系では、キャプチャ、エンコード、送出、描画までに計測点を設置し、方式差分（ハードウェア/ソフトウェアエンコード、フレームキュー戦略、GPUメモリ内完結とCPU readback）を A/B 比較した。  
入力系では、送信、Host受信、反映、表示までを別系統で計測し、Input-to-Photon を評価した。結果として、映像系はメモリ経路とキュー設計がテールレイテンシを支配し、入力系は反映タイミング設計が体感遅延を左右することを確認した。再現手順とともに知見を報告する。

## 1. 背景と問題設定
- RemoteRG の要件: 低遅延配信 + 入力同期
- なぜ「平均遅延」だけでは不十分か（P95/P99 が体感に効く）
- 今回の検証スコープ
  - セットA-1: エンコード実装比較（HW vs SW）
  - セットA-2: Codec比較（例: H.264 / VP8 / VP9）
  - セットB: FIFO vs 最新優先キュー
  - セットD: GPU内完結 vs CPU readback 経路
  - 入力遅延: Input-to-Photon の分解計測（映像系と分離）

## 2. システムと計測ポイント
### 2.1 データパス（映像系）
- Capture -> Encode -> WebRTC Send -> Decode/Render

### 2.2 データパス（入力系）
- Input(DataChannel) -> Host反映 -> 次フレームに反映 -> Android表示

### 2.3 計測タイムスタンプ定義（映像系）
- `t_cap`: キャプチャ直後
- `t_enc_in`: エンコーダ投入時刻
- `t_enc_out`: エンコード完了時刻
- `t_send`: 送信時刻
- `t_decode_out`: Android側でデコード済みフレームを取得した時刻（`VideoSink`時刻）
- `t_render`: Androidで表示された時刻（`VideoSink`受信時刻または`SurfaceTexture`更新時刻）

### 2.4 計測タイムスタンプ定義（入力系）
- `t_input_send`: クライアント入力送信時刻
- `t_input_recv`: Hostで入力受信時刻
- `t_input_applied_frame_cap`: 入力結果が最初に現れるフレームの `t_cap`

### 2.5 指標（映像系）
- E2E遅延: `t_render - t_cap`
- 補助指標:
  - エンコード時間: `t_enc_out - t_enc_in`
  - デコード時間: `t_decode_out - t_packet_recv`（取得可能な場合）
  - 受信からデコード済みまで: `t_decode_out - t_recv`（proxy指標）
  - readback時間（必要時）: `t_enc_in - t_cap` の内訳
  - CPU使用率（hostd / Androidアプリ）
  - フレームドロップ率

### 2.6 指標（入力系）
- Input-to-Photon遅延: `t_render - t_input_send`
- 分解指標:
  - Uplink: `t_input_recv - t_input_send`
  - 入力反映待ち: `t_input_applied_frame_cap - t_input_recv`
  - 反映後表示: `t_render - t_input_applied_frame_cap`

## 3. 実験条件（再現性のための固定項目）
- ハードウェア構成（CPU/GPU/メモリ）
- OS / ドライバ / Androidアプリバージョン
- ゲームシーン（静的/動的の2種類）
- ネットワーク条件（同一LAN、可能なら tc/netem 等で別途条件追加）
- 共通設定（解像度/FPS/ビットレートは固定）
- 実行回数: 各条件 3-5回、1回60秒

## 4. 比較実験
### 4.0 方針
- 本章は映像系のみを扱う（入力系は 5章で独立評価）

### 4.1 セットA: エンコーダ比較（HW vs SW）
- 目的: エンコーダ選択が遅延・CPUに与える影響を把握
- 期待: HW が平均とCPUで優位、ただし条件によりテール差を確認
- 出力: E2E の P50/P95/P99、デコード関連指標、CPU%、drop率

### 4.1.1 セットA-2: Codec比較（H.264 / VP8 / VP9 / AV1）
- 目的: Codec選択がエンコード/デコードコストと遅延テールに与える影響を把握
- 前提: 可能な限り同一ビットレート・同一FPSで比較し、実装方式（HW/SW）は固定条件を明記
- 期待:
  - H.264は実機互換性と低遅延運用で有利
  - VP8/VP9は条件次第で画質効率は良いが、端末側decodeコストが増える可能性
- 出力: E2E P50/P95/P99、EncodeLatency、DecodeProxy(またはDecodeLatency)、CPU%、drop率

### 4.2 セットB: キュー戦略比較（FIFO vs 最新優先）
- 目的: バックプレッシャー時の遅延増大を抑える設計を検証
- 期待: 最新優先は平均画質の一部を犠牲にしてテール改善
- 出力: 特に P95/P99 とドロップ率を重視

### 4.3 セットD: メモリ経路比較（GPU内完結 vs CPU readback）
- 目的: DirectXテクスチャ処理をCPUへ引き戻すコストを定量化
- 期待: readbackありは平均差よりテール悪化とCPU増加が目立つ
- 出力: readback時間、E2E P95/P99、CPU%、フレーム安定性

## 5. 入力遅延の独立評価
### 5.1 目的
- 入力遅延は映像系比較と独立に扱い、Input-to-Photon を分解して評価する

### 5.2 比較項目（入力系）
- 即時送信 vs 短時間バッチング（例: 8-16ms）
- 入力反映タイミングの設計差（受信即反映 / フレーム境界反映）

### 5.3 出力
- Input-to-Photon の P50/P95/P99
- Uplink / 反映待ち / 反映後表示 の内訳
- CPU%（必要なら）

## 6. 結果
### 6.1 グラフ構成（最低4枚）
- グラフ1: セットA-1（HW/SW）の E2E遅延分布（P50/P95/P99）
- グラフ2: セットA-2（Codec）の Encode/Decode指標比較
- グラフ3: セットB の E2E遅延分布（P50/P95/P99）
- グラフ4: セットD の readback時間 + デコード関連指標 + E2Eテール比較
- グラフ5: 入力系の Input-to-Photon 比較（P50/P95/P99 + 内訳）

### 6.2 テーブル
- 各条件（実装方式/Codec/キュー/メモリ経路）の中央値、P95、P99、デコード関連指標、CPU%、drop率を1表に集約

## 7. 考察
- なぜ平均よりテールが重要か（体感との対応）
- 局所最適ではなくパイプライン最適が効いた理由
- readbackが効率を崩すメカニズム（コピー、同期、CPU負荷）
- 入力系で支配的だった区間（送信/反映待ち/表示）の整理
- 品質・安定性・遅延のトレードオフ整理（映像系と入力系を分離）

## 8. 実運用に向けた推奨設定（暫定）
- 第一推奨: HWエンコード + 最新優先キュー + GPU内完結
- Codec方針: 低遅延重視では H.264 を基準にし、端末互換性とdecode負荷で最終決定
- フォールバック方針: SWエンコード時の安全設定
- 負荷スパイク時の劣化制御（画質より遅延優先）
- 入力系推奨: 即時送信を基本に、必要時のみ短時間バッチング

## 9. 制約と今後
- 今回未検証の項目（ネットワーク劣化条件、端末差、長時間安定性）
- 次の実験計画（WAN条件、熱影響、複数端末）

## 付録A: 計測ログフォーマット（例）
```json
{
  "frame_id": 12345,
  "t_cap": 1000.123,
  "t_enc_in": 1000.130,
  "t_enc_out": 1000.141,
  "t_send": 1000.142,
  "t_recv": 1000.170,
  "t_decode_out": 1000.180,
  "t_render": 1000.188,
  "input_id": 987,
  "t_input_send": 1001.001,
  "t_input_recv": 1001.010,
  "t_input_applied_frame_cap": 1001.020
}
```

## 付録B: 記事内で使う主要式
- `E2E = t_render - t_cap`
- `InputToPhoton = t_render - t_input_send`
- `EncodeLatency = t_enc_out - t_enc_in`
- `DecodeProxy = t_decode_out - t_recv`（Android側での近似）
- `ReadbackCost ~= t_enc_in - t_cap`（readback有無比較で解釈）

## 書き切り用チェックリスト
- [ ] 比較ごとに固定条件と可変条件を明記した
- [ ] 平均だけでなく P95/P99 を掲載した
- [ ] グラフと結論の対応関係を本文で説明した
- [ ] 「なぜその結果になったか」をパイプライン視点で説明した
- [ ] 再現可能な手順（環境・回数・時間）を記載した
